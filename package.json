{
  "name": "core-panel",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "drizzle-orm": "0.45.1",
    "turbo": "2.8.20",
    "typescript": "5.9.3"
  },
  "packageManager": "npm@11.6.2",
  "engines": {
    "node": ">=18.0.0"
  }
}
